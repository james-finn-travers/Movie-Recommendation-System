[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "hybrid_rust"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
bindings = "pyo3"
module-name = "hybrid_rust"
